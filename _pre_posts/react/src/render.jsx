var React = require('react');
var ReactDOM = require('react-dom');

var container = React.createClass({
    render: function () {
        return (
            <div>
                Hello World!
            </div>
        );
    }
});
